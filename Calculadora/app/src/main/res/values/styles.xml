<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="defaultButton">
        <item name="android:width">@dimen/btnDefaultWidth</item>
        <item name="android:height">@dimen/btnDefaultHeight</item>
        <item name="android:textSize">@dimen/btnDefaultFontSize</item>
        <item name="android:colorBackground">@color/powerOff</item>
    </style>

</resources>
